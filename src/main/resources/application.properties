spring.application.name=vulnscanner

# Fortify 실행 파일 경로 (비어있으면 시스템 PATH의 sourceanalyzer 사용)
fortify.executable-path=/Users/munchanghyeon/Documents/Workspce/SCA/SCA25.4/bin/sourceanalyzer

# Primary Database (VulnScanner)
spring.datasource.primary.url=jdbc:mariadb://localhost:3306/vulnscanner_db?createDatabaseIfNotExist=true&sslMode=DISABLED&serverTimezone=UTC
spring.datasource.primary.username=fortify
spring.datasource.primary.password=Fortify!234
spring.datasource.primary.driver-class-name=org.mariadb.jdbc.Driver

# Secondary Database (Mocha Dev)
spring.datasource.mocha.url=jdbc:mariadb://localhost:3306/mocha_dev?createDatabaseIfNotExist=true&sslMode=DISABLED&serverTimezone=UTC
spring.datasource.mocha.username=fortify
spring.datasource.mocha.password=Fortify!234
spring.datasource.mocha.driver-class-name=org.mariadb.jdbc.Driver

# H2 Database Configuration (Commented out)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=10GB
spring.servlet.multipart.max-request-size=10GB

# Session Cookie Security
server.servlet.session.cookie.http-only=true
# Set to true in production with HTTPS
server.servlet.session.cookie.secure=false

# SBOM API
sbom.api.url=http://localhost:5000
