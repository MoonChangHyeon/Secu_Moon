spring.application.name=vulnscanner

# Fortify 실행 파일 경로 (비어있으면 시스템 PATH의 sourceanalyzer 사용)
fortify.executable-path=/Users/munchanghyeon/Documents/Workspce/SCA/SCA25.4/bin/sourceanalyzer

# Database Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/vulnscanner_db?createDatabaseIfNotExist=true&sslMode=DISABLED&serverTimezone=UTC
spring.datasource.username=fortify
spring.datasource.password=Fortify!234
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# H2 Database Configuration
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Session Cookie Security
server.servlet.session.cookie.http-only=true
# Set to true in production with HTTPS
server.servlet.session.cookie.secure=false
